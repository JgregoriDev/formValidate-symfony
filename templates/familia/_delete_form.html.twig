<form method="post" action="{{ path('app_familia_delete', {'codfamilia': familium.codfamilia}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ familium.codfamilia) }}">
    <button class="btn">Delete</button>
</form>
