<div class="row">
	<div class="col-1"></div>
	<div class="col-10">
		{{ form_start(form) }}
		{{ form_widget(form) }}
		<div class="d-flex justify-content-center mb-3">
 			<a class="btn btn-secondary me-1"  href="{{ path('app_proveedor_index') }}">Volver a la lista</a>
			<button class="btn btn-{{typeButton}}">{{ button_label|default('Guardar') }}</button>
		</div>
		{{ form_end(form) }}
	</div>
	<div class="col-1"></div>
</div>
