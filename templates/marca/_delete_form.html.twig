<form method="post" action="{{ path('app_marca_delete', {'codmarca': marca.codmarca}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ marca.codmarca) }}">
    <button class="btn">Delete</button>
</form>
