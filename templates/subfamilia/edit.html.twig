{% extends 'base.html.twig' %}

{% block title %}Editar Subfamilia
{% endblock %}

{% block body %}
	{#{% if formBusqueda is defined %}
		{{form_start(formBusqueda)}}
		<div class="row">
			<div class="col-9">
				{{ form_widget(formBusqueda) }}
			</div>
			<div class="col-3">
				<button type="submit" class="d-inline btn btn-primary" title="Buscar">
					<i class="bi bi-search mx-2"></i>
				</button>
			</div>
		</div>
		{{form_end(formBusqueda)}}
	{% endif %}#}
	<h1>Editar Subfamilia</h1>
	<nav aria-label="breadcrumb my-3">
		<ol class="breadcrumb">
			<li class="breadcrumb-item">
				<a href="{{ path('app_inicio') }}">Inicio</a>
			</li>
			<li class="breadcrumb-item">
				<a href="{{ path('app_subfamilia_index') }}">Subfamilias</a>
			</li>
			<li class="breadcrumb-item active" aria-current="page">Editar subfamilia</li>
		</ol>
	</nav>
	<div class="d-flex justify-content-end">
		{{ include('subfamilia/_delete_form.html.twig') }}
	</div>
	{{ include('subfamilia/_form.html.twig', {'button_label': 'Actualizar'}) }}

	{# <a href="{{ path('app_subfamilia_index') }}" class="btn btn-secondary">Volver a la lista</a> #}


{% endblock %}
